# Building Custom User Authentication with Prisma

In this guide, we'll build a custom user authentication system from scratch using Prisma. We'll start with a fresh setup where we already have Prisma installed and configured.

## Database Configuration

Our Prisma schema is configured with SQLite as the database provider:

```

```
